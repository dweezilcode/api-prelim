# .env file

# JWT secret key for signing and verifying tokens
JWT_SECRET=000111

# Database credentials (example for PostgreSQL, update with your own values)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=one
DB_USER=root
DB_PASSWORD=root

# Server settings
PORT=4000

# Other environment variables as needed
NODE_ENV=production